<x-layouts.editor title="Thiết lập 2FA">
    <div class="mx-auto max-w-2xl space-y-6">
        <header><h2 class="font-headline-lg text-on-surface">Bật xác thực hai bước</h2><p class="mt-2 text-on-surface-variant">Quét mã QR bằng ứng dụng Authenticator, sau đó nhập mã 6 số để xác nhận.</p></header>
        @if ($errors->any())<div role="alert" class="rounded-xl border border-error/30 bg-error-container p-4 text-on-error-container">{{ $errors->first() }}</div>@endif
        <section class="space-y-5 rounded-xl border border-outline-variant bg-surface p-6"><div class="flex flex-col items-center gap-4 rounded-xl bg-surface-container-low p-5"><img src="{{ $qr }}" alt="Mã QR 2FA" class="size-56 rounded-lg bg-white p-2"><div class="text-center"><p class="text-sm text-on-surface-variant">Hoặc nhập khóa thủ công</p><code class="break-all font-mono text-sm text-on-surface">{{ $secret }}</code></div></div><form method="post" action="{{ route('editor.profile.2fa.confirm') }}" class="space-y-4">@csrf<label for="code" class="block text-sm font-medium text-on-surface-variant">Mã xác thực (6 số)<input id="code" name="code" type="text" inputmode="numeric" autocomplete="one-time-code" maxlength="6" required autofocus class="mt-1.5 h-11 w-full rounded-lg border border-outline-variant bg-surface px-3"></label><button class="rounded-lg bg-primary px-5 py-2.5 font-semibold text-on-primary">Xác nhận và tiếp tục</button></form><a href="{{ route('editor.profile.show', ['tab' => 'security']) }}" class="block text-sm text-primary hover:underline">Hủy — quay lại bảo mật</a></section>
    </div>
</x-layouts.editor>
