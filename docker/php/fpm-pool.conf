; Enable the ping/status endpoints for container healthchecks.
[www]
ping.path = /ping
ping.response = pong
pm.status_path = /status

; Pass through key runtime env vars to the FPM workers.
clear_env = no
